services:
  rutracker-mcp:
    build: .
    container_name: rutracker-mcp
    restart: always
    ports:
      - '3010:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./torrents:/app/torrents
      - ./.cookie:/app/.cookie
